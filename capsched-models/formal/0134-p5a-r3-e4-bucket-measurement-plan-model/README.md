# Formal 0134: P5A-R3 E4 Bucket Measurement Plan

Date: 2026-07-16

Status: pre-source evidence model. It authorizes only creation of the exact
disposable two-file E4 measurement draft after validation/0228 passes.

The model separates predecessor/source confinement, one-projection rq-lock
measurement, bounded hotplug drain, targeted fanout, evidence classification,
and source-draft authorization. `Safety` requires the exact E3 diagnostic pass;
unchanged E3 protocol; default-off same-TU disposable scope; no live scheduler
attachment; real rq locking; one projection and no leaf scan; preallocation;
stable-generation Fresh publication; paired alternating controls; saturating
difference; complete 32+5+5 cell matrices and 10,000 samples; immutable
5/25/50 microsecond, 25/50 microsecond, 10/100 millisecond, and 700 microsecond
gates; warning rejection; preserved negative evidence; malformed-evidence
failure; arm64 plus x86_64 same-source runs; virtualization claim limits; no
post-failure range reduction; and no premature E5, cross-path, or production
claim.

Validation generates forty independent unsafe configurations from the
canonical fault list in the plan JSON. Each removes one indispensable contract
family and must produce an expected `Safety` counterexample.
