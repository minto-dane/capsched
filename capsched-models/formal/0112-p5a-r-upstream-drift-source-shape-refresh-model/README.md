# P5A-R Upstream Drift Source-Shape Refresh Model

This model checks the gate used after upstream/master advanced from
`87320be9...` to `71dfdfb...`.

It allows the narrow ordinary-CFS-only `0009` draft to remain reviewable only
when the direct P5A-R scheduler source-shape is unchanged, merge-tree is clean,
and lifecycle/global freshness is not claimed from this evidence.
