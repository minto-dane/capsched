# Formal 0145: P5A-R6 Post-E3 Authorization and Threat Boundary

Status: source-free authorization model awaiting canonical validation

Date: 2026-07-26

## Purpose

Model the separately reviewed transition after the closed R6-E3 four-profile
synthetic evidence.

The safe transition requires the repository threat model, governing plan,
source gate, complete matrix, two independent closures, exact claim-ledger
row, upstream drift observation and classification, clean merge tree, and
exact disposable source scope. It then accepts only:

```text
the exact existing default-off disposable R6-E3 source identity
the exact synthetic protocol semantics under the recorded four virtual boots
drafting a separate source-free R6-E4 plan
```

It does not accept an R6-E4 plan or source, live scheduler attachment, primary
or patch-queue mutation, runtime behavior, runtime budget, async/service,
MemoryView/TLB, device/DMA/IOMMU, monitor, cluster, bare-metal, performance,
production, deployment, multi-cluster, or datacenter claims.

Twenty-four unsafe configurations independently remove one prerequisite or
add one forbidden authorization/claim. The model proves only authorization
alignment with the recorded evidence and threat boundary. It does not prove
Linux runtime behavior or monitor-backed protection.
