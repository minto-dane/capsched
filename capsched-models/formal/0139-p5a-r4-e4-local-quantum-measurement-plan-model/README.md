# Formal 0139: P5A-R4 E4 Local-Quantum Measurement Plan

Date: 2026-07-18

Status: source-free pre-source authorization model. It can authorize only an
exact disposable, default-off, same-translation-unit E4 measurement draft.

The safe trace binds the post-N-135 authorization, exact candidate and
claim-ledger row; freezes the two-file/default-off/E3-preserving boundary;
requires real callback and lock contexts, paired alternating controls,
saturating subtraction, complete 682-cell matrices, 10,000 pairs per cell,
fixed local/offline/async rejection limits, logical work counts, preserved
negative evidence, two-architecture same-source classification, and the
absence of a global settlement gate; then permits source drafting without
permitting measurement launch or any behavior, runtime, monitor, production,
or cluster claim.

Validation/0257 generates one configuration for each of the 43 canonical
faults in the plan JSON. Each fault removes one prerequisite or introduces one
forbidden authorization/claim and must produce an expected `Safety`
counterexample. The model does not prove Linux runtime behavior or timing; it
proves only that the plan authorization cannot exceed its evidence contract.
