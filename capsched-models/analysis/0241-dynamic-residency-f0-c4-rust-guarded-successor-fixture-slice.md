# 0241 — Candidate-4 Rust guarded-successor fixture slice

## Disposition

The neutral hostile transport now asks the Rust refinement and normative Python
model for the complete sorted action/actor multiset available from an exact
state.  It distinguishes a valid terminal state (accepted with an empty set)
from an invalid state (rejected by the public `next_states` guard).  The slice
passes and advances conservative original-case mapping to 98 of 295; 197 remain,
all six refinement gates stay open, G6 retry remains false, and G7 remains
blocked.

## Query semantics

Fixture kind `N` carries one complete 54-field state without a verdict.  Python
invokes normative `next_states`.  Rust first applies its independent
`InstanceWF` predicate, then invokes its handwritten transition generator only
for a well-formed state.  Both sides either emit explicit rejection or the
canonical sorted multiset of `(action_id, actor)` pairs.  Multiplicity is
preserved; successor ordering is not promoted into semantics.

This boundary covers facts that a single positive/negative edge cannot:

- descendant and asynchronous reacquisition enable/disable boundaries;
- protection, quota, owner-revocation, and runtime-failover action sets;
- final-counter and evidence-seal availability ceilings;
- pending hostile-attempt reject/bypass alternatives;
- protection closure only after a pending attack is rejected;
- all five breached states are valid terminal states; and
- an unsealed quiescent state is rejected rather than mistaken for terminal.

## Accounting

The corpus contains 108 queries: 10 grant, 49 state, 22 edge, and 27 guarded
successor queries.  Fixture-local metadata marks original-credit mappings; the
10 earlier positive edge witnesses remain supplemental.  Therefore
`case_count=108`, `mapped_original_case_credits=98`, and
`supplemental_case_count=10` are separate quantities.  Fixture SHA-256 is
`5a9efc84e9c4ab10cfcf3a43dd0f67836e11c717de174a3e21a80848776f292c`;
result SHA-256 is
`d96367e3f2ae5e16cc7d65d52a4bf36ae40226abf8f5eaeb1500870e7e98cf89`.
Eighteen malformed transports fail closed.

## Authority and next work

The query runs only in offline differential validation and does not change the
normative model, Linux, Monitor, or the Rust bounded graph hot path.  Python
remains authoritative.  Next, add neutral two-order trace/commutation and
projection-relation queries, then map the remaining terminal-certificate and
representation assertions before claiming all 295 cases.
